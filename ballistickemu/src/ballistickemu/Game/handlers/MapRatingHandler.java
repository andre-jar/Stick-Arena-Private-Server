package ballistickemu.Game.handlers;

import ballistickemu.Types.StickClient;

public class MapRatingHandler {
	public static void HandlePacket(StickClient client, String Packet) {
		// String rating = Packet.substring(2);
		// if(!client.getLobbyStatus()) {
		// String map = client.getRoom().getMapID();
		// System.out.println("Client " +client.getName() + " rated Map ID:" +map + "
		// with Rating " +rating);
		// }
	}
}
